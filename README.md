# OpenCode Themes

Custom themes for OpenCode TUI.

## Installation

Copy theme files to `~/.config/opencode/themes/` or project `.opencode/themes/` directory.

## Usage

Use `/theme` command in OpenCode TUI or set `theme` in `tui.json`.